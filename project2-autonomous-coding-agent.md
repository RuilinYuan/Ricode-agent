# 项目二：自主编程 Agent

## 项目背景

用户输入编程需求，Agent 自主完成代码生成、执行、报错分析、修复、测试验证的完整闭环，无需人工介入中间步骤。核心挑战在于三点：长轮次执行中上下文快速膨胀导致任务中断、高并发场景下重复计算系统 prompt 成本高、以及 Agent 陷入无进展修复循环的失控问题。

---

## 系统架构

```
用户需求输入
   ↓
任务规划 & 代码生成
   ↓
┌────────────────────────────────┐
│      执行-观察-修复 主循环       │
│                                │
│  ┌──────────────────────────┐  │
│  │      上下文压缩层          │  │
│  │  Token 水位监控            │  │
│  │  L1 持久化 → L2 剪裁       │  │
│  │  L3 清理推理 → L4 分段摘要  │  │
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │      缓存优化层            │  │
│  │  稳定前缀 / 状态层 / 动态尾部│  │
│  │  缓存有效期时间压缩机制     │  │
│  └──────────────────────────┘  │
│                                │
│  ┌──────────────────────────┐  │
│  │      循环检测层            │  │
│  │  N-gram 推理相似度检测     │  │
│  │  工具调用指纹轨迹分析       │  │
│  │  分级告警 / 纠偏 / 熔断    │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘
   ↓
任务完成 / 输出结果
```

---

## 核心模块设计

### 1. 基于 Token 水位的分层上下文压缩

**问题**

Agent 每轮将代码执行结果、报错日志、测试输出完整写入上下文。编程任务平均需要 20～40 轮工具调用，运行至中期 Token 占用已接近窗口上限，导致任务被迫中断或关键历史信息被截断丢失。

**设计**

持续监控当前上下文的 Token 水位，按水位阈值逐级触发压缩操作，信息损失从低到高依次递进：

| 压缩级别 | 触发水位 | 操作 | 信息损失 |
|---------|---------|------|---------|
| L1 | 60% | 工具返回结果持久化到外部存储，context 中仅保留摘要引用 | 极低 |
| L2 | 75% | 剪裁早期历史轮次，保留关键决策节点与重要中间结果 | 低 |
| L3 | 85% | 清理推理过程（thinking）内容，仅保留结论与行动 | 中 |
| L4 | 95% | 对历史内容分段生成摘要，以摘要替换原始对话 | 可控 |

压缩优先级设计原则：优先压缩信息密度低的内容（如重复的执行日志），保留决策节点和关键报错信息。

**效果**

上下文占用降低 XX%，单会话持续运行轮次提升 XX%。

---

### 2. 分层缓存布局与时间压缩机制

**问题**

项目结构说明、代码规范、依赖环境描述等系统 prompt 在每轮对话中内容不变，但每当动态内容更新时缓存前缀即失效，导致这部分内容被反复重新计算。高并发场景下多个任务共享相同的系统 prompt，重复计算成本尤为突出。

**设计**

按内容更新频率将 prompt 拆分为三层，分别管理缓存策略：

```
┌─────────────────────────────────────┐
│  稳定层（极少变动，作为缓存前缀）      │
│  系统指令 / 代码规范 / 项目结构描述    │
├─────────────────────────────────────┤
│  状态层（任务级更新）                 │
│  当前任务目标 / 已完成步骤摘要         │
├─────────────────────────────────────┤
│  动态层（轮次级更新，作为动态尾部）    │
│  最新工具执行结果 / 当前轮推理内容     │
└─────────────────────────────────────┘
```

- **稳定前缀固定化**：将稳定层内容置于 prompt 最前端，确保其作为缓存前缀不受动态内容影响
- **时间压缩机制**：结合不同模型的缓存有效期（如 Anthropic 为 5 分钟），在缓存即将到期前主动触发一次轻量请求刷新缓存，避免过期后的冷启动重算
- **跨任务复用**：同一项目的多个并发任务共享稳定层缓存，摊薄重复计算成本

**效果**

缓存命中率提升 XX%，输入 Token 成本降低 XX%。

---

### 3. 无进展循环检测与分级熔断

**问题**

Agent 遇到难以修复的报错时，容易陷入"修改代码 → 执行 → 同样报错 → 再修改"的死循环。由于每轮模型输出表面上看起来不同（修改了不同的代码位置），简单的文本去重无法识别，只能依赖超时强制终止，既浪费大量 Token，又无法输出当前最优结果。

**设计**

从推理内容和工具调用行为两个维度同时检测循环：

**检测维度一——推理内容 N-gram 相似度**
- 对连续 N 轮的模型推理输出提取 N-gram 特征
- 计算相邻轮次间的相似度，超过阈值则判定推理陷入重复

**检测维度二——工具调用指纹轨迹**
- 对每次工具调用的参数 + 执行结果计算哈希指纹
- 在滑动窗口内分析调用序列，识别以下三种无进展模式：
  - **重复调用**：同参数同工具反复执行，结果无变化
  - **无进展轮询**：结果每次有微小变化但无实质推进（如报错行号变化但根因相同）
  - **交替调用**：A→B→A→B 的周期性模式，在两种方案间反复横跳

**分级响应策略**

```
轻度（连续 3 轮触发）
  → 告警记录 + 向 prompt 注入引导信息（"当前方向已尝试多次，请换思路"）

中度（连续 5 轮触发）
  → 策略纠偏：强制切换修复策略，或回滚至上一个执行成功的稳定状态重新规划

重度（连续 8 轮触发）
  → 自动熔断：终止当前循环，输出截至当前的最优可用结果，并附上失败原因说明
```

**效果**

无效调用减少 XX%，循环检测准确率达到 XX%。

---

## 项目总结

| 模块 | 解决的核心问题 | 关键技术 | 效果 |
|------|-------------|---------|------|
| 分层上下文压缩 | 长任务 Token 膨胀导致任务中断 | Token 水位监控 + 四级压缩策略 | 上下文占用降低 XX%，可运行轮次提升 XX% |
| 分层缓存优化 | 系统 prompt 重复计算，成本线性增长 | 稳定前缀布局 + 缓存时间压缩 | 缓存命中率提升 XX%，Token 成本降低 XX% |
| 循环检测与熔断 | Agent 陷入无进展死循环，Token 浪费 | N-gram 相似度 + 调用指纹 + 分级熔断 | 无效调用减少 XX%，检测准确率达 XX% |

---

## 关于效果数据

XX% 的指标建议通过以下方式获取：

- **上下文压缩**：构造 20～30 个典型编程任务，对比压缩前后的 Token 使用量与任务完成轮次
- **缓存命中率**：通过模型 API 返回的 usage 字段统计 `cache_read_input_tokens` 占比
- **循环检测准确率**：构造一批已知会陷入循环的 case，手工标注后计算准确率与召回率
